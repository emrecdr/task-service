# RECEIPT-1 — clarification questions for the devt arch-scan feedback (task-service)

<!-- generated by devt calibration session 5d8338aa (2026-07-27); answers feed the fix batches for your 9-finding report -->

Your report was code-verified 9/9 CONFIRMED — several findings with sharper root causes
than reported (the architect dispatch is a raw prose template, not a compiled region,
which is *why* the contract gates were structurally blind to it). Mechanical fixes are
proceeding; these questions target the decisions where your field detail changes the
design. Evidence over impressions: cite run artifacts (the state file you improvised,
`claim-check-failures.jsonl`, `hook-trace/run-hook.jsonl`, `status.json`, scratchpad)
wherever possible.

## A. Stateless shell (your #1 — the carrier design)

1. The state file you improvised for `$SCAN_ID`/`$SCANNER_COMMAND`: where did you put it
   and what shape (one file per value, one env-style file, JSON)? Did any step need a
   value computed MORE than one step earlier, or is every reuse adjacent-step?
2. Beyond arch-health-scan.md: did you notice cross-fence `$VAR` reuse in any other
   workflow you touched this run? (The lint will sweep all of them regardless — this
   tells us which to hand-verify first.)
3. Would collapsing each step into ONE fenced block have sufficed for your run, or do
   values genuinely need to cross step boundaries (e.g. scan-id used again at finalize)?

## B. Headless behavior (your #2 — defaults)

4. Was your run actually blocked interactively, or is "in an autonomous run it would
   block" extrapolated? If blocked: at the scanner-wiring prompt, the triage prompt, or both?
5. For per-finding triage in a headless run, which default is correct: defer-ALL new
   findings and surface in the report, or auto-accept into baseline with a delta note?
   (Proposed config: `arch_scanner.autowire: never|ask|auto`, default `ask` — right shape?)

## C. Claim-check lifecycle (your #3)

6. After the SendMessage-resume produced `arch-review.md`, which exact command cleared
   the failure record (`state assert-artifact-present architect`? `post-dispatch-check`?) —
   paste the invocation + output line.
7. Proposed fix: the finalize gate re-probes artifact presence itself and appends the
   success record silently (self-heal), failing only when the artifact is STILL missing.
   Acceptable, or do you want recovery to stay a visible, logged operator action?

## D. Dispatch contract (your #4 — the envelope we'll compile)

8. Paste the `scope_trust`/`scope_hint`/`memory_signal` blocks you synthesized for the
   architect dispatch. Did the architect visibly consume them (cited scope files,
   downgraded trust, referenced memory docs)? Your improvised shape becomes the template's
   starting point if it worked.
9. `{models.architect}`: what did you substitute, and would balanced-profile resolution
   (opus for architect) have matched your intent? Did you consult `models get balanced`
   or frontmatter to decide?
10. Paste the exact `state validate` output that flagged the artifact-name mismatch —
    the specific expected-vs-found line pins which map rows are fossils.

## E. Identity + stop-stamp (your #5/#6 — severity calibration)

11. From your `hook-trace/run-hook.jsonl` or the SubagentStop payload: which fields did
    the payload actually carry (`agentName`? `subagent_type`? neither)? The fallback
    chain design depends on what your build emits.
12. The stop-stamp during in-flight work: was the live agent the RESUMED architect
    (running across a turn boundary via SendMessage)? Did anything downstream misbehave
    from the "stopped" state — a `/devt:next` misroute, a gate misfire — or was it
    cosmetic state-file wrongness only?

## F. Untailored rules (your #8 — pick the fix you'd actually use)

13. Rank these: (a) installed templates carry a TAILOR-ME stamp + `--health` nags while
    untailored; (b) `--health` diffs rules against CLAUDE.md and reports contradictions;
    (c) setup offers an LLM-assisted tailoring pass that rewrites templates against
    CLAUDE.md at init; (d) a precedence line in every template ("project CLAUDE.md wins
    on conflict") and nothing else. Which would you have actually used, and which would
    have prevented the architect near-miss?
14. Which memory doc did the `memory_signal` carry that kept the architect from grading
    against the bad rules? (This is the value receipt for that machinery — cite the doc id.)
15. Did any OTHER dispatch this session (reviewer, curator) read the contradicting rules
    before you noticed, or was the architect the only exposure?

## G. Evolution + polish (your #9)

16. At 24 commits/1 author the coupling degrees hit 91%: should small-history coupling be
    ANNOTATED low-confidence (still shown) or SUPPRESSED below a floor (like ownership's
    solo_or_small_team gate)? What floor felt right — commits, authors, or both?
17. status.json history: what did you need the earlier entries FOR (reconstructing the
    failure timeline? auditing dispatch order?) — does a ring buffer of the last N
    entries suffice, or append-only JSONL?

## H. Net calibration

18. The 6 knowledge candidates in scratchpad (incl. the Cosmic-Python REJ): did you run
    the curator promote flow? Did it work, and headless or interactive?
19. Cost/value: rough dispatch count + wall-clock for the full gated scan vs what a
    plain single-agent "review my architecture" would have cost you. Where does your
    keep/tune line sit — and would you run this again on Phase 2 of this project?
20. What is the #1 remaining friction AFTER the nine findings are fixed — and did
    anything about the run get WORSE as you used it more?
